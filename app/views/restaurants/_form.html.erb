<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name%>
  <%= f.input :address%>
  <%= f.input :category, placeholder: "('chinese', 'italian', japanese, 'french' and 'belgian')" %>
  <%= f.submit "Add", class: "btn btn-primary" %>
<% end %>
